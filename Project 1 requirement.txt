clear instructions
// I need a backend that allows discord 0auth2. 



// This will read users roles to make sure they have the specific role to be allowed access to the site.
We don't need everyone to have access, we only want users with certain role in our discord to have access


// Connect a blank React application which includes a NavBar with a 'Login' btn, once logged in a 'Logout' btn.